"use client"
import React, { useState } from 'react'

const page = () => {

  const [department, setDepartment] = useState([]);

  const getDepartment = async () => {
    await fetch('')
  }

  return (
    <div>
        
    </div>
  )
}

export default page;